namespace Imperial2030.Shared.Constants;

public static class ManeuverRules
{
    // Armies a nation must sacrifice to destroy a foreign factory (ManeuverController.DestroyFactory).
    public const int DestroyFactoryArmyCost = 3;
}
